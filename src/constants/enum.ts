export enum UserIdentifier {
  PhoneNumber = 'phone_number',
  Email = 'email',
  UserId = 'user_id',
  MongoId = '_id',
}

export enum UserRoles {
  ADMIN = 'ADMIN',
  AUTHOR = 'AUTHOR',
  USER = 'USER'
}
